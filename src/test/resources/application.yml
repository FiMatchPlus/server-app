spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

    # MongoDB 설정 (테스트용)
    data:
      mongodb:
        host: localhost
        port: 0  # 0으로 설정하면 랜덤 포트 사용 (Embedded MongoDB)
        database: test_stockone

    mongodb:
      embedded:
        version: 6.0.5  # MongoDB 버전 지정

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect